<script type="text/javascript">
    $(function() {
        $("#table_facebox")
            .tablesorter({
                theme: 'blue',
                widthFixed: true,
                widgets: ['zebra', 'filter']
            })
            .tablesorterPager({
                container: $(".pager4"),
                output: '{startRow} - {endRow} / {filteredRows} ({totalRows})',
                fixedHeight: true,
                removeRows: false,
                cssGoto: '.gotoPage'
            });
    });
</script>

<div class="content-header"></div>
<div class="UI_Header_Title">
    <h1><?= $this->avatar; ?> Pilih Nama Mentor</h1>
</div>
<div class="clear"></div>
<br>

<? if (count($this->rows) > 0) { ?>
    <?= $this->pager4; ?>

    <table cellspacing="1" cellpadding="6" width="100%" class="tablesorter" id="table_facebox">
        <thead>
            <tr>
                <th width="5px">No.</th>
                <th>Nama Mentor</th>
                <th>Aksi</th>
            </tr>
        </thead>
        <tbody>
            <?
            $i = 1;
            // $url="http://".$_SERVER['HTTP_HOST'].$_SERVER['REQUEST_URI'];
            foreach ($this->rows as $row) {
                echo '<tr>';
                echo '<td align="center">' . $row->id . '</td>';
				echo '<td>' . $row->nama_mentor . '</td>';
                echo '<td align="center"><a href="javascript:selectFields4(\'' . $row->id . '\', \'' . $row->id . '\', \'' . $row->nama_mentor . '\', \'' . basename($_SERVER['REQUEST_URI']) . '\')">PILIH</a></td>';
                echo '</tr>';

                $i++;
            }
            ?>
        </tbody>
    </table>
<? } else {
    echo 'Data tidak ditemukan.';
}
?>